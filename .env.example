# Backend API URL — used as proxy target in dev, and direct baseURL in production build
# In development: vite proxies /api → VITE_API_URL (no CORS issues)
# In production: set this to your deployed backend URL
VITE_API_URL="https://your-backend.onrender.com"
